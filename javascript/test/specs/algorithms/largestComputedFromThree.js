/* jslint node:true, mocha:true */
var assert = require('chai').assert,
    largestComputedFromThree = require('../../../algorithms/largestComputedFromThree'),
    testData = {
        input: [-10, 7, 29, 30, 5, -10, -70],
        output: 21000
    };

describe('compute largest product from 3 integers', function () {
    // it('')
});
